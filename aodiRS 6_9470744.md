# 奥迪RS 6 操作指南
  
更新时间：2026年07月08日00时03分26秒   

## 一、启动与熄火
### 启动发动机
1.  **智能钥匙**：确保钥匙在车内。
2.  **踩下制动踏板**：必须用力踩下制动踏板。
3.  **按下一键启动按钮**：位于方向盘右侧，中控台下方。按住约1秒，发动机将启动。
4.  **注意**：如果仪表盘显示“未识别到钥匙”，请将钥匙紧贴转向柱右侧的感应区。
### 熄火
1.  **停稳车辆**：挂入P挡。
2.  **按下启动按钮**：再次按下即可熄火。
3.  **电子手刹**：熄火后，部分车型会自动拉起电子手刹（位于挡杆左下方的 `(P)` 按钮）。也可手动上拉。
## 二、驾驶模式选择 (Audi Drive Select)
RS 6 的核心在于其可调驾驶模式。通过中控台上的 `DRIVE SELECT` 按钮或MMI屏幕操作。
| 模式 | 适用场景 | 特性 |
| :--- | :--- | :--- |
| **舒适** | 日常通勤、长途 | 悬架最软，转向轻盈，排气声浪低沉，油门响应平顺。 |
| **自动** | 日常驾驶 | 系统根据驾驶风格自动调整，兼顾舒适与性能。 |
| **动态** | 激烈驾驶、山路 | 悬架变硬，转向更重，油门响应极快，排气阀门全开，声浪高亢。 |
| **效率** | 节油巡航 | 优化换挡时机，降低空调功率，滑行模式激活。 |
| **RS1 / RS2** | 赛道/自定义 | **核心模式**。可自定义发动机、悬架、转向、quattro运动差速器、排气等所有参数。RS2模式下可关闭ESC。 |
**操作建议**：
- 方向盘上的 `RS MODE` 按钮（若有）可一键切换至预设的RS1/RS2模式。
- 在MMI中进入 `车辆 > 驾驶模式选择` 进行详细个性化设置。
## 三、变速箱操作 (8速Tiptronic)
- **D挡 (前进)**：正常驾驶，偏向经济舒适。
- **S挡 (运动)**：将挡杆向后拉一下即可切换。换挡时机延后，保持高转速，动力随叫随到。
- **手动模式**：
    - 将挡杆向右推入 `M/S` 槽位。
    - 使用方向盘换挡拨片 (`-` 降档，`+` 升档)。
    - 仪表盘会显示当前档位。
- **换挡拨片技巧**：
    - 在D/S挡下直接拨动拨片可临时进入手动模式，几秒无操作后自动恢复自动模式。
    - 长按 `+` 拨片可退出临时手动模式。
## 四、RS 6 特有功能操作
### 1. RS 运动排气
- **控制方式**：通过中控台上的 `EXHAUST` 按钮（排气管图标）或MMI菜单开启/关闭。
- **效果**：开启后，排气旁通阀打开，声浪明显增大，回火放炮声更清晰。
- **建议**：在动态或RS模式下，排气默认开启。
### 2. 自适应空气悬架
- **高度调节**：在MMI中可手动升高或降低车身。
- **自动调节**：
    - 高速行驶时（约120km/h以上），车身自动降低10mm。
    - 在动态模式下，车身会额外降低20mm。
    - 通过烂路时，可手动升高车身。
- **注意**：升高/降低过程中，仪表盘会有指示灯闪烁。
### 3. quattro 运动差速器
- **作用**：主动分配后轮左右扭矩，实现极致的弯道性能。
- **操作**：无需手动操作，由系统自动控制。在动态或RS模式下，介入更积极，允许轻微甩尾。
## 五、仪表盘与HUD抬头显示
### 虚拟座舱 (Audi Virtual Cockpit)
- **视图切换**：通过方向盘左侧的 `VIEW` 按钮切换。
- **RS专属视图**：提供转速表居中、功率/扭矩表、圈速计时器、G值表等性能界面。
- **操作**：在 `RS1/RS2` 模式下，仪表盘会显示专属RS布局。
### 抬头显示 (HUD)
- **开启/调节**：`MMI > 车辆 > 显示与亮度 > 抬头显示`。
- **显示内容**：车速、导航箭头、主动安全警告、转速（需在RS模式下设置）。
## 六、驾驶辅助系统
### 自适应巡航 (ACC)
- **开启**：方向盘左侧 `SET` 按钮。
- **调节车距**：`←`/`→` 按钮。
- **调节速度**：`+`/`-` 按钮（每按一次±1km/h，长按±10km/h）。
- **暂停/恢复**：`CANCEL` 暂停，`RES` 恢复。
### 车道保持辅助
- **开启/关闭**：方向盘左侧灯光控制杆顶端的按钮。
- **提示**：系统会主动修正方向盘，保持车辆在车道中央。变道前务必打转向灯，否则会感到方向盘阻力。
## 七、停车与驻车
1.  **停车**：踩死制动踏板。
2.  **挂入P挡**：按下挡杆顶端的 `P` 按钮。
3.  **电子手刹**：上拉电子手刹按钮 `(P)`。
4.  **自动驻车 (Auto Hold)**：按下挡杆下方的 `AUTO HOLD` 按钮（指示灯亮起）。等红灯时深踩刹车，仪表盘显示绿色 `HOLD`，即可松开刹车。轻踩油门自动解除。
## 八、应急操作
### 钥匙没电
- 取出钥匙内的机械钥匙（按下钥匙侧面的释放按钮）。
- 用机械钥匙撬开驾驶座门把手下方的盖板，插入机械锁孔开门。
- 进入车内后，将钥匙紧贴转向柱右侧的感应区，然后正常启动。
### 拖车模式
- 车辆必须处于P挡或N挡。
- 拖车钩位于前后保险杠下方的盖板内，按压盖板边缘即可打开。
## 九、维护提示
| 项目 | 建议周期 |
| :--- | :--- |
| **机油** | 建议使用0W-20或5W-30（符合VW 508 00标准），每1万公里或1年更换。 |
| **变速箱油** | 每6万公里检查，建议8万公里更换。 |
| **quattro差速器油** | 每3万公里检查。 |
| **刹车盘/片** | RS 6车重较大，刹车损耗较快，建议每2-3万公里检查。 |
| **轮胎** | 建议使用高抓地力性能胎（如P Zero Corsa），注意胎压（参考油箱盖内侧标签）。 |
| **颗粒捕捉器 (GPF)** | 避免长期短途低速行驶，偶尔高转速行驶有助于再生。 |
---
**安全驾驶提醒**：RS 6 动力强劲，请务必在合法合规的路况下体验其性能。关闭ESC（电子稳定程序）驾驶仅在赛道等封闭场地进行。  

参考资料：   
pg.m8a.pro <br>
rg.3li7.cn <br>
fr.yeluedu.cn <br>
zd.3li7.cn <br>
ma.1qc2.cn <br>
l6.coin19.cn <br>
zt.6tc6.cn <br>
xd.coin18.cn <br>
tg.6gl8.pro <br>
d4.6gl8.pro <br>